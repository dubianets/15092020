import React from "react";

export default function Refresh(props){

            // <button onClick={props.addCounter1}>Add counter</button>

    return(

        <div>
            <button onClick={props.refreshAll}>RefreshAll</button>
        </div>

    )
}